global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Alerting rules
rule_files:
  - /etc/prometheus/prometheus-alerts.yml

scrape_configs:
  - job_name: 'go-template-api'
    static_configs:
      - targets: ['api:3000']
    metrics_path: /metrics
    scheme: http
